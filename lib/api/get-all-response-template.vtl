#set($inputRoot = $input.path('$'))
{
  "v": 16,
  "total": $input.path('$.Count'),
  "items": [
    #foreach($elem in $inputRoot.Items) {
        "id": $elem.product-id.N,
        "name": "$elem.name.S",
        "price": $elem.price.N,
        "sales": $elem.sales.N
    }#if($foreach.hasNext),#end
    #end
  ]
}